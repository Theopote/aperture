diff --git a/aperture-runtime/src/main/java/dev/aperture/runtime/ApertureRuntime.java b/aperture-runtime/src/main/java/dev/aperture/runtime/ApertureRuntime.java
--- a/aperture-runtime/src/main/java/dev/aperture/runtime/ApertureRuntime.java
+++ b/aperture-runtime/src/main/java/dev/aperture/runtime/ApertureRuntime.java
@@ -9,0 +10,2 @@
+import dev.aperture.runtime.pipeline.RuntimeInteraction;
+import dev.aperture.runtime.pipeline.RuntimeResult;
@@ -10,0 +13,2 @@
+
+import java.util.UUID;
@@ -26,0 +30 @@
+	private final ArchitecturalRuntimeEnvironment environment;
@@ -36 +41,2 @@
-		RuntimePipeline runtimePipeline
+		RuntimePipeline runtimePipeline,
+		ArchitecturalRuntimeEnvironment environment
@@ -45,0 +52 @@
+		this.environment = environment;
@@ -89,0 +97,16 @@
+
+	public ArchitecturalRuntimeEnvironment environment() {
+		return environment;
+	}
+
+	public RuntimeResult interact(UUID objectId, RuntimeInteraction interaction) {
+		return environment.interact(objectId, interaction);
+	}
+
+	public void scheduleInteraction(long delayTicks, UUID objectId, RuntimeInteraction interaction) {
+		environment.scheduleInteraction(delayTicks, objectId, interaction);
+	}
+
+	public int tick() {
+		return environment.tick();
+	}
diff --git a/aperture-fabric/src/main/java/dev/aperture/bootstrap/ApertureBootstrap.java b/aperture-fabric/src/main/java/dev/aperture/bootstrap/ApertureBootstrap.java
--- a/aperture-fabric/src/main/java/dev/aperture/bootstrap/ApertureBootstrap.java
+++ b/aperture-fabric/src/main/java/dev/aperture/bootstrap/ApertureBootstrap.java
@@ -19,0 +20 @@
+import dev.aperture.runtime.ArchitecturalRuntimeEnvironment;
@@ -20,0 +22 @@
+import dev.aperture.runtime.diagnostic.RuntimeDiagnostics;
@@ -21,0 +24 @@
+import dev.aperture.runtime.event.RuntimeEventBus;
@@ -27,0 +31,5 @@
+import dev.aperture.runtime.replication.RuntimeReplicator;
+import dev.aperture.runtime.schedule.RuntimeTickScheduler;
+import dev.aperture.runtime.state.RuntimeObjectRegistry;
+import dev.aperture.runtime.transaction.RuntimeTransactionManager;
+import dev.aperture.runtime.world.RuntimeWorldQuery;
@@ -53,4 +62,17 @@
-	private final RuntimePipeline runtimePipeline = new RuntimePipeline(
-		List.of(new OpeningRuntimeBehavior(openingTypes)),
-		new OpeningInstanceRepository(instances)
+	private final OpeningInstanceRepository runtimeState = new OpeningInstanceRepository(instances);
+	private final RuntimeObjectRegistry runtimeObjects = new RuntimeObjectRegistry(runtimeState);
+	private final RuntimePipeline runtimePipeline = new RuntimePipeline(
+		List.of(new OpeningRuntimeBehavior(openingTypes)), runtimeObjects
+	);
+	private final ArchitecturalRuntimeEnvironment runtimeEnvironment = new ArchitecturalRuntimeEnvironment(
+		runtimeObjects,
+		runtimePipeline,
+		new RuntimeTransactionManager(),
+		new RuntimeEventBus(),
+		new RuntimeTickScheduler(),
+		RuntimeWorldQuery.empty(),
+		RuntimeReplicator.noop(),
+		new RuntimeDiagnostics()
 	);
@@ -70 +92,2 @@
-			runtimePipeline
+			runtimePipeline,
+			runtimeEnvironment
