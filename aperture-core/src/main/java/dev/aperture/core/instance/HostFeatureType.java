package dev.aperture.core.instance;

/** Kind of host sub-element to which an architectural object is attached. */
public enum HostFeatureType {
	FACE, EDGE, OPENING, PATH, VOLUME, GRID, NAMED_ANCHOR
}
