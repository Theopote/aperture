package dev.aperture.core.instance;

/** Geometric and lifecycle policy used to attach an object to its host feature. */
public enum HostAttachmentMode {
	FREE_STANDING, SURFACE_MOUNTED, EDGE_ATTACHED, CUT_THROUGH, PATH_FOLLOWING, CONNECTING, CONTAINED
}
