package dev.aperture.core.editor.constraint;

/**
 * Spatial editor constraints distinct from parametric {@code ConstraintRule} expressions.
 */
public enum EditorConstraintKind {
	FIT_HOST,
	NO_OVERLAP,
	MAINTAIN_ASPECT,
	LOCK_AXIS
}
